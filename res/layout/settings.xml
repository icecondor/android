<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" 
                  android:key="first_preferencescreen">  
                  <!--  -->
  <DialogPreference android:key="authentication" 
                      android:title="Reporting Authentication"
                      android:dialogTitle="Authentication Token"
                      android:dialogMessage="The authentication token allows position updates to be sent to a location storage provider."
                      android:persistent="false"
                       />
  <ListPreference android:key="transmission frequency"
                  android:title="Reporting Frequency"
                  android:entries="@+array/time_frequency"
                  android:entryValues="@+array/time_frequency_values" />
  <ListPreference android:key="rss read frequency"
                  android:title="GeoRSS Read Frequency"
                  android:entries="@+array/time_frequency"
                  android:entryValues="@+array/time_frequency_values" />
  <!-- <EditTextPreference android:key="transmission frequency"
                      android:title="Upload Frequency"
                      android:entries="@+array/time_frequency"
                      android:entryValues="@+array/time_frequency_values" />
   <CheckBoxPreference android:key="wifi enabled" 
                      android:title="Use WiFi Location" /> -->
</PreferenceScreen>
